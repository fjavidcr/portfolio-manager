--- src/features/transactions/components/AddTransactionForm.vue
+++ src/features/transactions/components/AddTransactionForm.vue
@@ -142,6 +142,12 @@
         return
       }

+      if (description.value.length > 500) {
+        alert('Description must be 500 characters or less')
+        submitting.value = false
+        return
+      }
+
       await updateTransaction(routeId.value, {
         type: type.value,
         amount: normalizedAmount,
@@ -160,6 +166,12 @@
         return
       }

+      if (description.value.length > 500) {
+        alert('Description must be 500 characters or less')
+        submitting.value = false
+        return
+      }
+
       await addDoc(collection(db, 'users', uid, 'transactions'), {
         userId: uid,
         type: type.value,
@@ -351,6 +363,7 @@
             placeholder="0.00"
             step="0.01"
             min="0"
+            max="1000000000"
           />
         </div>
       </div>
@@ -402,6 +415,7 @@
           rows="3"
           class="block w-full pl-10 pr-3 py-3 text-on-surface bg-surface border border-outline-variant focus:outline-none focus:ring-2 focus:ring-primary focus:border-primary sm:text-sm rounded-xl transition-all duration-200"
           placeholder="Optional notes..."
+          maxlength="500"
         ></textarea>
       </div>
     </div>
